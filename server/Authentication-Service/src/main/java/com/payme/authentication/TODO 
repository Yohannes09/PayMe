GENERAL:
- Utilize the cache more for things like Users,
  something to get more use out of the cache


TESTING:
- User login of various states (locked, banned, valid, etc)
- User registration (duplicate credentials, etc)
- ensure you cannot register with "    " (any blank credential)








// Useful for seeing what beans were scanned

//    @Bean
//    public CommandLineRunner checkContext(ApplicationContext ctx) {
//        return args -> {
//            System.out.println("Beans:");
//            Arrays.stream(ctx.getBeanDefinitionNames())
//                    .filter(name -> name.contains("dummy") || name.contains("controller"))
//                    .sorted()
//                    .forEach(System.out::println);
//        };
//    }