spring:
  datasource:
    url:
    driverClassName:
    username: user
    password: password
  h2:
    console:
      enabled: true
  jpa:
    database-platform:
    hibernate:
      ddl-auto: update
    show-sql: true